// WAP to find a the answer given formula (x-y)2.

#include<stdio.h>

void main()
{
    int x,y,z;
    printf("Enter value of x = ");
    scanf("%d",&x);

    printf("Enter value of y = ");
    scanf("%d",&y);

    // z = (x*x) + (y*y) - 2*x*y;
    z = (x-y) * (x-y);
    printf("(x-y)2 = %d"
}
